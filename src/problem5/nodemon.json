{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "execMap": {
    "ts": "ts-node -r dotenv/config -r tsconfig-paths/register src/app.ts"
  },
  "env": {
    "NODE_ENV": "development"
  }
}
